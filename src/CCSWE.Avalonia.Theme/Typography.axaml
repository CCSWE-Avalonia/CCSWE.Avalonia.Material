<!--
  =============================================================================
  AUTO-GENERATED by CCSWE Design System (Avalonia) v1.7.0
  Tokens 1.1.0
  Do not edit by hand - regenerate from tokens/ (tokens.upstream-1.1.0.json + tokens.local.json).
  =============================================================================

  Material 3 type scale (15 roles), emitted as TextBlock style classes.
  Apply with Classes, e.g. <TextBlock Classes="HeadlineSmall" .../>.

  Family: brand (Plus Jakarta Sans) for display / headline / titleLarge;
  body (DM Sans) for everything else. Metrics resolved verbatim from
  tokens.upstream-1.1.0.json -> typography.role.

  letterSpacing: emitted as an ABSOLUTE DIP equal to the upstream value
  (TextBlock.LetterSpacing is an absolute DIP offset; the upstream "em" label
  is a historical misnomer - every consumer applies the figure as absolute
  tracking, matching M3's published per-role values and Android's `.sp`).
  Roles whose tracking is 0 omit the setter (0 is the default).

  Foreground is intentionally NOT set here - type roles inherit color from
  their context (a Filled button supplies OnPrimary, body text supplies
  OnSurface, etc.), per the M3 contract.
-->
<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Display -->
  <Style Selector="TextBlock.DisplayLarge">
    <Setter Property="FontFamily" Value="{StaticResource BrandFontFamily}" />
    <Setter Property="FontSize" Value="57" />
    <Setter Property="LineHeight" Value="64" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="LetterSpacing" Value="-0.25" />
  </Style>
  <Style Selector="TextBlock.DisplayMedium">
    <Setter Property="FontFamily" Value="{StaticResource BrandFontFamily}" />
    <Setter Property="FontSize" Value="45" />
    <Setter Property="LineHeight" Value="52" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>
  <Style Selector="TextBlock.DisplaySmall">
    <Setter Property="FontFamily" Value="{StaticResource BrandFontFamily}" />
    <Setter Property="FontSize" Value="36" />
    <Setter Property="LineHeight" Value="44" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>

  <!-- Headline -->
  <Style Selector="TextBlock.HeadlineLarge">
    <Setter Property="FontFamily" Value="{StaticResource BrandFontFamily}" />
    <Setter Property="FontSize" Value="32" />
    <Setter Property="LineHeight" Value="40" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>
  <Style Selector="TextBlock.HeadlineMedium">
    <Setter Property="FontFamily" Value="{StaticResource BrandFontFamily}" />
    <Setter Property="FontSize" Value="28" />
    <Setter Property="LineHeight" Value="36" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>
  <Style Selector="TextBlock.HeadlineSmall">
    <Setter Property="FontFamily" Value="{StaticResource BrandFontFamily}" />
    <Setter Property="FontSize" Value="24" />
    <Setter Property="LineHeight" Value="32" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>

  <!-- Title -->
  <Style Selector="TextBlock.TitleLarge">
    <Setter Property="FontFamily" Value="{StaticResource BrandFontFamily}" />
    <Setter Property="FontSize" Value="22" />
    <Setter Property="LineHeight" Value="28" />
    <Setter Property="FontWeight" Value="SemiBold" />
  </Style>
  <Style Selector="TextBlock.TitleMedium">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="16" />
    <Setter Property="LineHeight" Value="24" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="LetterSpacing" Value="0.15" />
  </Style>
  <Style Selector="TextBlock.TitleSmall">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="LineHeight" Value="20" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="LetterSpacing" Value="0.1" />
  </Style>

  <!-- Body -->
  <Style Selector="TextBlock.BodyLarge">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="16" />
    <Setter Property="LineHeight" Value="26" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="LetterSpacing" Value="0.5" />
  </Style>
  <Style Selector="TextBlock.BodyMedium">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="LineHeight" Value="23" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="LetterSpacing" Value="0.25" />
  </Style>
  <Style Selector="TextBlock.BodySmall">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="LineHeight" Value="20" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="LetterSpacing" Value="0.4" />
  </Style>

  <!-- Label -->
  <Style Selector="TextBlock.LabelLarge">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="LineHeight" Value="20" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="LetterSpacing" Value="0.1" />
  </Style>
  <Style Selector="TextBlock.LabelMedium">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="LineHeight" Value="16" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="LetterSpacing" Value="0.5" />
  </Style>
  <Style Selector="TextBlock.LabelSmall">
    <Setter Property="FontFamily" Value="{StaticResource BodyFontFamily}" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="LineHeight" Value="16" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="LetterSpacing" Value="0.5" />
  </Style>

</Styles>
