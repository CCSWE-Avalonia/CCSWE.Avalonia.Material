name: Build, test, and publish

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

permissions:
  contents: read

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_NOLOGO: true
  Configuration: Release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0   # Nerdbank.GitVersioning needs full history

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore src/CCSWE.Avalonia.Material.slnx

      - name: Build
        run: dotnet build src/CCSWE.Avalonia.Material.slnx -c Release --no-restore /p:ContinuousIntegrationBuild=true

      - name: Test
        run: |
          if [ -n "$(find . -name '*.Tests.csproj' -o -name '*.UnitTests.csproj' 2>/dev/null)" ]; then
            dotnet test src/CCSWE.Avalonia.Material.slnx -c Release --no-build --verbosity normal
          else
            echo "No test projects yet — skipping."
          fi

      - name: Pack
        run: dotnet pack src/CCSWE.Avalonia.Material/CCSWE.Avalonia.Material.csproj -c Release --no-build -o "${{ github.workspace }}/artifacts"

      - name: Upload package artifacts
        uses: actions/upload-artifact@v7
        with:
          name: nupkg
          path: ${{ github.workspace }}/artifacts/*

      - name: Publish to NuGet.org
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        run: >
          dotnet nuget push "${{ github.workspace }}/artifacts/*.nupkg"
          --api-key ${{ secrets.NUGET_API_KEY }}
          --source https://api.nuget.org/v3/index.json
          --skip-duplicate
